// TIPOS DE DADOS PRIMITIVOS = STRING, NUMBER, BOOLEAN, NULL, UNDEFINED, SYMBOL
const nome = 'João'; // string
const idade = 20; // number
const peso = 80.20; // number
let nomeAluno // undefined - não aponta para local nenhum na memória
const sobrenomeAluno = null // nulo - não aponta para local nenhum na memória
const aprovado = true; // boolean - true ou false (lógico)	

const alunos = ['João', 'Maria', 'José']; // array
const alunos2 = alunos // valor por atribuição
alunos2.push('José'); // adiciona um novo elemento no array